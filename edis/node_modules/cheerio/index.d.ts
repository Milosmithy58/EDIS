export type CheerioSelection = {
  each(callback: (index: number, element: any) => void): CheerioSelection;
  find(selector: string): CheerioSelection;
  text(): string;
  attr(name: string): string | undefined;
};

export type CheerioAPI = ((selector: string) => CheerioSelection) & CheerioSelection;

export function load(html: string): CheerioAPI;
